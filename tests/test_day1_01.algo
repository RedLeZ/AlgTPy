lire(x)
c <- 12 <= 10
x <- x + 1.98
si x > 12 alors
  ecrire("nice")
  si c == faux alors
    ecrire("omg")
  finSi
sinon si x < 12 alors
  ecrire("mid")
sinon
  ecrire("not nice")
finSi
ecrire(c)
ecrire(x)

